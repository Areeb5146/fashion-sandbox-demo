{
  "name": "ai-fashion-sandbox-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Micro-trial POC: 3-layer parametric garment sandbox with Catmull-Rom splines, dependency graph, and soft-wall constraints.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-cascade.mjs"
  },
  "dependencies": {
    "fabric": "^5.3.0",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
